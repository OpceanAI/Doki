FROM node:22-alpine

WORKDIR /app

COPY server.js .

EXPOSE 8080

HEALTHCHECK --interval=5s --timeout=3s --retries=3 \
  CMD wget -q --spider http://localhost:8080 || exit 1

CMD ["node", "server.js"]
